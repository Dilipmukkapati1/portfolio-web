# Copy to `.env.local` for auth credentials when running against local portfolio-api.
# API URL / household are set by `npm run dev:local` — no need to edit here.

AUTH_USERNAME=admin
AUTH_PASSWORD=portfolio
AUTH_DISPLAY_NAME=Admin
AUTH_SECRET=portfolio-dev-secret

NEXT_PUBLIC_APP_URL=http://localhost:3000
